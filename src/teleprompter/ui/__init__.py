"""Qt user interface.

Windows and pages subscribe to :class:`~teleprompter.state.AppState` and
:class:`~teleprompter.state.PlaybackController` signals. No widget reaches into
another widget's attributes.
"""
